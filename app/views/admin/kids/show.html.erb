<h1><%= @kid.name %></h1>

<p><%= link_to "Edit", edit_admin_kid_path(@kid) %></p>
<p><%= link_to "Delete Kid", admin_kid_path(@kid), method: :delete,
    data: {confirm: "Are you sure you want to delete #{@kid.name}?"} %></p>
